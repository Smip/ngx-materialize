import {TapTargetDirective} from './tap-target.directive';

describe('TapTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new TapTargetDirective();
    expect(directive).toBeTruthy();
  });
});
