import {ChipsDirective} from './chips.directive';

describe('ChipsDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipsDirective();
    expect(directive).toBeTruthy();
  });
});
