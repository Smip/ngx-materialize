import {PushpinDirective} from './pushpin.directive';

describe('PushpinDirective', () => {
  it('should create an instance', () => {
    const directive = new PushpinDirective();
    expect(directive).toBeTruthy();
  });
});
