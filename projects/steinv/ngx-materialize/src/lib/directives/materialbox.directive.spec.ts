import {MaterialboxDirective} from './materialbox.directive';

describe('MaterialboxDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialboxDirective();
    expect(directive).toBeTruthy();
  });
});
